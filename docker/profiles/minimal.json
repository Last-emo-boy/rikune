{
  "$schema": "../docker-profile.schema.json",
  "name": "minimal",
  "description": "Core static analysis only — no external tools. Smallest image, fastest build.",
  "plugins": [
    "pe-analysis", "strings", "static-triage", "code-analysis",
    "binary-diff", "cross-module", "elf-macho",
    "reporting", "visualization", "batch"
  ]
}

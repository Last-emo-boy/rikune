# Optional dynamic-analysis direct dependencies for CPython 3.12 on linux/amd64.
# Install only in isolated lab environments using the complete hashed closure in
# requirements-dynamic.lock.txt; these remain separate from the static baseline.
# Note: the PyPI package named `speakeasy` is an unrelated metrics server.
# Use `speakeasy-emulator`, which provides the `speakeasy` malware-emulation module.
# The exact beta is the first compatible line using maintained Unicorn 2.x.

speakeasy-emulator==2.0.0b4
frida==17.8.0
frida-tools==14.6.1
psutil==7.2.2
pandare==1.8.83

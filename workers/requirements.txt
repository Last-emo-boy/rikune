# Python worker direct dependencies for CPython 3.12 on linux/amd64.
# Production installs use the root requirements.lock.txt, which resolves this
# file together with ../requirements.txt as one dependency environment.
pefile==2024.8.26
lief==0.17.5
yara-python==4.5.4
yara-x==1.14.0
flare-floss==3.1.1
dnfile==0.18.0
z3-solver==4.13.4.0

# Optional dynamic-analysis stack is maintained in requirements-dynamic.txt
